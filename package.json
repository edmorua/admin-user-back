{
  "name": "user-admin-back",
  "version": "1.0.0",
  "description": "back to handle the crud for user",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-dev": "NODE_ENV=development node server.js",
    "start-prod": "NODE_ENV=production node server.js"
  },
  "author": "edmorua",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.9.25",
    "sha1": "^1.1.1"
  }
}
